/**
 * NOTA: La funcionalidad del slider ha sido deshabilitada
 * Mantener este archivo para compatibilidad con otros scripts
 */
document.addEventListener('DOMContentLoaded', function() {
    // No inicializar nada - slider eliminado
});

/**
 * Función vacía para mantener compatibilidad con llamadas existentes
 */
function updateProductosData() {
    // No hacer nada - slider eliminado
}
